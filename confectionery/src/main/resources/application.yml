spring:
  application:
    name: confectionery
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/confectionery_db
    username: root
    password: maksio1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 128MB
      max-request-size: 128MB
      enabled: true
logging:
  level:
    org.springframework.security: TRACE